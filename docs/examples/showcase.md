# MarkdownViewer

**Bold**, *italic*, ~~struck~~, `inline code`, and
[links](https://marked.js.org) — rendered fully offline.

| Mode | Shortcut |
|---|---|
| Preview | `⌘1` |
| Edit | `⌘2` |
| Split | `⌘3` |

```js
// Driver-pane scroll sync: only the
// pane you're driving propagates.
editor.onwheel = () => (driver = editor);
```

- [x] Live reload when the file changes on disk
- [ ] Your next great document

> Open, read, edit, save — done.
