# Screenshots

Drop PNGs here with these exact names so they appear in the main `README.md`:

| Filename | What it should show |
|---|---|
| `split.png` | Split mode — raw markdown on the left, rendered preview on the right |
| `ai-chat.png` | The AI chat panel docked beside the document |
| `about.png` | The About window (icon, name, Changelog / Architecture buttons) |
| `settings.png` | The AI Settings window (provider, base URL, model, API key) |

Tip: on macOS, `⇧⌘4` then press **Space** and click a window to capture just that window
with a clean drop shadow (transparent background) — ideal for these.
